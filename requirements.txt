# VOCALOID6 MCP 服务器依赖（独立于流水线 requirements.txt）
# mcp 必须 <2.0：2.x 移除了 mcp.server.fastmcp（FastMCP），1.29.x 为稳定版
mcp>=1.3,<2.0
pretty_midi>=0.2.10
numpy>=1.24
# render_wav（方案 B）：pywinauto UI 自动化渲染导出 WAV
pywinauto>=0.6.8
